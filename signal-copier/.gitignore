.env
*.db
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Real config, copied from the .example.yaml files — may end up naming real
# accounts/webhooks even though secrets themselves live in env vars, not here.
config/accounts.yaml
config/routing.yaml
config/providers.yaml

# Deployment IaC state/secrets -- never commit
deploy/terraform/**/.terraform/
deploy/terraform/**/*.tfstate
deploy/terraform/**/*.tfstate.backup
deploy/terraform/**/terraform.tfvars
deploy/cloudflare-heartbeat/.dev.vars
deploy/litestream/litestream.env
