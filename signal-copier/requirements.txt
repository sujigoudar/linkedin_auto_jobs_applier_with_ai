fastapi>=0.111
uvicorn[standard]>=0.30
pyyaml>=6.0
pydantic>=2.7
httpx>=0.27      # used by SignalStackBroker and AlpacaBroker (plain REST, no SDK)
python-multipart>=0.0.9  # required by FastAPI's Form() parsing, used by the /sms/twilio route

# Optional, needed only for adapters you actually enable:
# ccxt>=4.3                    # crypto exchange execution (CCXTBroker)
# python-telegram-bot>=21.0    # Telegram source
# discord.py>=2.4              # Discord source
# slack-bolt>=1.19             # Slack source
# twilio>=9.0                  # SMS source (request signature validation)
# tweepy>=4.14                 # Twitter/X source
# MetaTrader5>=5.0.45          # MT5 broker, same-host Windows only
# ib_insync>=0.9.86            # IBKR broker

pytest>=8.2
pytest-asyncio>=0.23
